Put your wallet credentials here. This folder is git-ignored.

Apple Wallet
  pass.p12   — Pass Type ID certificate exported from Keychain Access (with its private key)
  wwdr.pem   — Apple Worldwide Developer Relations intermediate certificate

Google Wallet
  google-service-account.json — service account key from Google Cloud Console

See ../README.md for step-by-step instructions.
